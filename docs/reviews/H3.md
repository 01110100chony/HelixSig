# H3 Review

## Scope and frozen contracts
H2 TECH_PASS integrated locally at a252293. User authorized H3 then H4.
See H3_PACKET.md, ARCHITECTURE.md and ROADMAP.md. One production writer and
separate-context review of a frozen candidate are required.

## Implementation handoff
- Candidate code commit: pending freeze commit after the passing local gate.
- Changes: bounded crossbeam input/result queues, scoped producer/workers,
  non-waiting batch fill, collector reuse, stop/drain/join paths, per-worker HDR
  aggregation, memory preflight allowance and independent Python readback.
- Known limitations: concurrent drop-new and Ctrl+C remain H4; no benchmark or
  remote CI claim. Worker panic containment exists but H4 adds fault-injection
  acceptance. External review is pending.

## Acceptance matrix
| Requirement | Evidence | Result |
|---|---|---|
| W=1/2/4, Q=1/256, B=1/16/64, both FFI modes | 12 concurrent runs plus 12 sequential references; NumPy/Parquet comparison by ID | PASS |
| Minimal runs, tails, 4101 events, graceful joins | scripts/validate_concurrent.py, each process timeout 90 seconds | PASS |
| Per-worker histogram aggregation and memory bounds | Rust metric/memory tests and per-worker/merged JSON checks | PASS |
| Prior runtime and FFI regressions | 18 Rust tests; 42 H2 CLI cases | PASS |
| Native/oracle/ASan/UBSan regression | 533 NumPy cases per native profile plus CTest | PASS |
| Separate-context external AI review | Pending frozen candidate | NOT_RUN |

## Verification
`HELIX_PYTHON=.venv/bin/python scripts/verify.sh H3` exited 0 on WSL2 Ubuntu 24.04.
Evidence: artifacts/h3-gate-initial.log; readback artifacts h3-wi8ml3tk and
h2-1a0lk4en. The removed H2 rejection of concurrent execution is superseded by
H3's successful concurrent acceptance; no mandatory validation was weakened.
The Windows mount reported sub-second make clock-skew warnings; unchanged native
sources passed CTest/oracle in Release and sanitized builds.

## Findings
Copilot planning incorrectly assumed fixed full batches under concurrent
scheduling; rejected by leader against try_recv semantics. The delegated test
script also assumed an undeclared metrics.batches field and removed temporary
artifacts; leader corrected it to the actual histogram schema and retained run
artifacts before running the gate. Independent gate review remains pending.

## Re-review history
Copilot auto produced test planning and a bounded test script in an isolated
worktree, using prompts limited to listed local files and tools. Source was
inspected and adjusted by the leader before integration. Separate-context gate
review is required on the frozen candidate.

## Technical verdict
CANDIDATE. Automated checks pass; external review/adjudication is pending.

## Learning checkpoint
PENDING_USER_DEMONSTRATION

## Handoff to next milestone
Not authorized by the technical gate yet.

