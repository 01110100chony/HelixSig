# H2 Review

## Scope and frozen contracts

H1 TECH_PASS is the prerequisite, explicitly accepted by the user. H2 follows
[H2_PACKET.md](H2_PACKET.md), ARCHITECTURE.md and DEVELOPMENT.md: finite sequential
replay, validated bounded corpus input, shared event/batch preparation, Parquet,
accounting and bounded metrics. Native numerical and FFI contracts are unchanged.
Concurrency, overload scheduling, interrupts and panic supervision remain H3/H4.

The user authorized parallel subagents. One leader wrote code; read-only workers
designed adversarial tests and reviewed frozen candidates. Final adjudication and
the technical verdict remained with the leader. These were AI reviews.

## Implementation handoff

- Production candidate: `f235e7f300752db75aa48e12c8a0a89061176175`.
- Final reviewed candidate: `cb35222a44e82d0413dd70023447a1ff7bd93385`.
  The intervening diff adds tests only; production code is identical.
- Changes: config/CLI, bounded corpus loading, owned events, reusable flat
  processing buffer, bounded HDR metrics, sequential runtime, Parquet writer,
  final JSON, Python readback/failure acceptance and H2 CI command.
- Dependencies: clap for CLI, serde/serde_json for JSON, sha2 for integrity,
  parquet for serialization without Arrow/compression features, hdrhistogram for
  bounded latency observations; tempfile is test-only. Cargo.lock is committed.
- Environment: WSL2 Ubuntu 24.04, repository at `/mnt/c/projetos2026/curriculo/Helix`,
  Rust 1.98.1, GCC 13.3, Clang 18.1.3, NumPy 2.5.3 and PyArrow 25.0.1.
- Limits: local correctness evidence, no remote CI run or performance claim.
  FIFO fixtures use Linux temporary storage because the Windows mount cannot
  create FIFOs. Human learning and publication approval are separate and pending.

## Acceptance matrix

| Requirement | Evidence | Result |
|---|---|---|
| Native numerical/FFI regressions | Release and Clang ASan/UBSan CTest; 533 NumPy cases in each profile; 7 existing Rust FFI tests | PASS |
| Formatting/lint | cargo fmt --check; clang-format --dry-run --Werror; cargo clippy --all-targets --locked -- -D warnings | PASS |
| Runtime and FFI tests | 9 H2 unit tests and 7 H1 integration tests | PASS |
| Complete readback and FFI parity | 4101 rows per mode, exact IDs/schema/features, row groups 4096+5, independent NumPy tolerance unchanged | PASS |
| Bounds and invalid configuration | 43-case Python suite: exact 64 MiB corpus and 64 KiB manifest acceptance; dimensions/hash/length/schema/path/config rejection; memory estimate rejection above 256 MiB | PASS |
| Existing output preservation | CLI existing-directory sentinel and OutputWriter create_new collision tests | PASS |
| Numerical failure accounting | Single/all NaN rows in both FFI modes, valid partial/empty Parquet, exact ID sets and counters | PASS |
| Output startup failure | Missing-parent CLI case; running-marker write error after directory creation; output-file open collision | PASS |
| Writer finalization/accounting | Injected group/close/rename failures, actual RLIMIT_FSIZE mid-group write failure; unfinalized rows remain unwritten | PASS |
| Summary publication failure | Unit test with actual Parquet/File; CLI stdout counters, valid partial output read by PyArrow, no trusted final JSON | PASS |
| Timing and metrics | Direct admission-clock and shared batch-completion assertions; delayed sink excludes writing from latency; empty/overflow histogram coverage and throughput identities | PASS |
| Separate-context AI review | Technical review, parallel evidence audit, leader adjudication and focal re-review of final candidate | PASS |

The 43 CLI scenarios also cover batch sizes 1/64, final tails, baseline override,
queue capacity 1, sequential drop-new without drops, and rejection of concurrent
execution. Small runs check IDs directly; production keeps no unbounded ID map.

## Verification

Full automated acceptance command:

```bash
HELIX_PYTHON=.venv/bin/python scripts/verify.sh H2
```

At `f235e7f`, this exited **0** with a clean tracked tree. Evidence:
`artifacts/h2-f235e7f-gate.log`, including 14 Rust tests, 41 Python scenarios,
Release/native oracle and mandatory standalone ASan/UBSan checks.
SHA-256: `b416ada61e4dc4f8bbd4476d750d737a9eb37615c97907201cade84d3717aa8c`.
Generated readback artifacts: `artifacts/h2-47g_enjj/`.

After adding review-requested tests (committed as `cb35222`), all affected checks
were rerun and exited **0**:

```bash
cargo fmt
cargo clippy --all-targets --locked -j 2 -- -D warnings
cargo test --locked -j 2
cargo build --locked -j 2
.venv/bin/python scripts/validate_pipeline.py --binary target/debug/helix --corpus fixtures/small
```

Evidence: `artifacts/h2-review-tests-final.log`, 16 Rust tests and 43 Python cases.
SHA-256: `68472bbb57ebcba34b0e23c565788d356a9619277c5be47e041aacf6dc720892`.
Generated readback artifacts: `artifacts/h2-yd__3n8q/`.
Native production/build/FFI sources were unchanged, so the full native and
sanitizer evidence above remains applicable to the final candidate.

The real writer-failure case uses a 16 KiB file limit with batch size 7. The
pinned uncompressed PLAIN writer flushes at 4096 rows; its first column exceeds
the limit. Exactly ceil(4096/7)*7 = 4102 attempts are accepted and processed;
written=0 and unwritten=4102. No later events are materialized. This is failure
accounting evidence, not benchmark evidence.

Initial unsuccessful attempts are retained in ignored artifacts: a Clippy API
adjustment, an unsupported FIFO on the Windows mount, and a new test that wrongly
assumed the Parquet constructor flushes its buffered header. The latter was
replaced with an actual file-open failure test after inspecting the pinned
dependency; group/footer error propagation remains tested. None is counted as a
passing gate. CI now invokes H2, but remote execution is NOT_RUN.

## Findings and adjudication

| ID | Reviewer classification | Leader adjudication | Resolution |
|---|---|---|---|
| H2-A1 | IMPORTANT: startup coverage incomplete | VALID evidence improvement; no production defect established | Added CLI running-marker failure and actual output-open collision coverage; PASS |
| H2-A2 | IMPORTANT: summary failure tested only in memory / overstated evidence | INVALID as stated: existing unit test uses real File, footer and rename, and docs did not claim all tests were end-to-end | Added CLI stdout/partial-file/PyArrow acceptance anyway; PASS |
| H2-A3 | IMPORTANT: admission/preparation/completion timing evidence incomplete | VALID evidence improvement; static trace already showed packing before FFI and timestamps after return | Added explicit admission-clock and shared batch-completion assertions; PASS |
| H2-A4 | Limitation: 4102 failure point nondeterministic | INVALID for the locked PLAIN writer and specified test; reviewer withdrew concern | Explained full row-group flush and ceil(4096/7)*7; no implementation change |

No BLOCKER/IMPORTANT finding remains unresolved. Architectural, numerical, FFI
or scope changes were neither requested nor made.

## Re-review history

1. Native Codex worker `h2_test_matrix` (gpt-5.6-luna, medium) independently
   proposed the adversarial matrix while the leader implemented. This was test
   planning, not gate review.
2. Candidate `f235e7f` was committed and frozen. Native workers
   `h2_frozen_review` (gpt-5.6-sol, medium) and `h2_evidence_audit`
   (gpt-5.6-luna, medium) reviewed in parallel with the full automated gate.
   The technical reviewer reported no actionable BLOCKER/IMPORTANT defect;
   the evidence auditor supplied the findings adjudicated above.
3. After both reviews ended, the leader added tests only, reran affected checks
   and froze `cb35222`. Both workers performed focal re-review of the diff and
   logs. The technical reviewer found no remaining BLOCKER/IMPORTANT findings;
   the evidence auditor confirmed no remaining concrete mandatory H2 gap.
4. Reviewers inspected code/diffs/logs without independently rerunning commands.
   Hostile concurrent filesystem mutation, power loss, native fatal faults and
   OOM behavior are not guaranteed. Runtime concurrency and interrupts remain
   future milestones. No human review or model-diverse external review is claimed.

The external Copilot wrapper dispatch was rejected by automatic approval review
because sending repository contents to that service lacked sufficiently explicit
payload authorization. It did not execute. Native subagents provided the
separate-context reviews; no workaround invoked the rejected external service.

## Technical verdict

**TECH_PASS — 2026-09-10.** H2 mandatory acceptance is supported by executable
evidence on the identified candidates, separate-context AI reviews, and closed
adjudications. Final documentation records this decision without changing code.

## Learning checkpoint

PENDING_USER_DEMONSTRATION. The Portuguese study guide contains an H2 walkthrough;
its presence does not establish user learning or public-release approval.

## Handoff to next milestone

H2 is complete. H3's technical prerequisite is satisfied; its bounded-runtime
work packet remains to be defined before implementation. H3 is NOT_STARTED.

