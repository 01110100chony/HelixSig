# H2 Review

## Scope and frozen contracts
Prerequisite H1 TECH_PASS achieved. H2 development authorized per `docs/reviews/H2_PACKET.md`.
See ARCHITECTURE.md, DEVELOPMENT.md, ROADMAP.md and H2_PACKET.md. H2 implements
finite sequential replay only. Native numerical/FFI contracts remain unchanged.
Concurrent runtime, overload scheduling and interrupt/panic supervision remain
H3/H4 scope. The user authorized read-only delegation alongside the sole code writer.

## Implementation handoff
- Candidate code commit: pending candidate freeze after automated acceptance.
- Changes: validated CLI/config, bounded corpus loader, owned events, common flat
  processing buffer for both FFI modes, bounded HDR metrics, sequential runtime,
  Parquet output/finalization, JSON summaries, Python acceptance script and H2 CI.
- New dependencies have concrete roles documented in README.md; Cargo.lock pins
  the resolved graph. No Arrow/compression feature is enabled in the Rust writer.
- Environment: WSL2 Ubuntu 24.04, repository on `/mnt/c`, Rust 1.98.1,
  GCC 13.3, Clang 18.1.3, NumPy 2.5.3 and PyArrow 25.0.1.
- Limitations: local validation only; no remote CI or performance/publication
  claim. FIFO validation uses Linux temporary storage because the Windows mount
  does not support FIFO creation. The running marker is retained as history;
  only final summary.json supersedes it.

## Acceptance matrix
| Requirement | Evidence | Result |
|---|---|---|
| Native numerical/FFI regressions | Release and sanitized CTest; 533 NumPy cases in each native profile | PASS |
| Rust formatting/lint | cargo fmt --check; cargo clippy --all-targets --locked -- -D warnings | Pending final candidate |
| Runtime/FFI unit and integration tests | cargo test --locked | Pending final candidate |
| 4101-event readback and FFI parity | scripts/validate_pipeline.py; IDs, schema, features, row groups 4096+5 | Pending final candidate |
| Invalid source/config and memory bounds | Python rejection matrix, exact 64 MiB/64 KiB limits and memory preflight | Pending final candidate |
| Numerical failure accounting | One/all NaN rows in both FFI modes; valid partial/empty Parquet | Pending final candidate |
| Writer failure/finalization | Injected Write/close/rename/summary errors; Linux RLIMIT_FSIZE mid-group failure | Pending final candidate |
| Metrics | Empty/overflow histogram tests; delayed sink excludes write time from latency | Pending final candidate |
| Separate-context AI review | Required after candidate freeze | NOT_RUN |

## Verification
Acceptance command: `HELIX_PYTHON=.venv/bin/python scripts/verify.sh H2`.

Initial full gate: artifacts/h2-gate-initial.log. Native checks passed; Clippy
reported chunks_exact_to_as_chunks in the loader, corrected without changing
semantics. Initial pipeline run reached the FIFO case and exposed a Windows-mount
limitation; the fixture now uses Linux temporary storage. These initial attempts
are not evidence of a complete H2 gate pass.

## Findings
No reviewer findings yet; independent review not performed.

## Re-review history
- A read-only native Codex worker (gpt-5.6-luna, medium) independently designed
  the adversarial matrix before implementation. This is test planning, not gate review.
- External Copilot wrapper dispatch was rejected by automatic approval review
  because sending repository contents to the external service lacked sufficiently
  explicit payload authorization. It did not execute; native review is the fallback.

## Technical verdict
CANDIDATE_IN_VALIDATION. Separate-context AI review remains required.

## Learning checkpoint
PENDING_USER_DEMONSTRATION

## Handoff to next milestone
Not authorized by the technical gate yet.

