# H5 Review

## Scope and frozen contracts
See ARCHITECTURE.md, ROADMAP.md, EXPERIMENTS.md and H5_PACKET.md.
H4 TECH_PASS recorded at a13eca1; H5 implementation is authorized.

## Implementation handoff
- Candidate code commit: NOT_CREATED
- Changes: Rust/C++ measurement drivers, bounded Linux campaign runner, independent
  numerical readback, provenance/RSS capture, analysis and CI smoke integration.
- Known limitations: full gate, frozen-candidate review and campaign pending.

## Acceptance matrix
NOT_RUN

## Verification
Initial candidate 32c2730 passed the complete H5 gate in a clean Linux checkout:
`HELIX_PYTHON=.venv/bin/python scripts/verify.sh H5`, exit 0, including mandatory
native sanitizers, all H4 regressions, five runner tests and 200 Release smoke
runs. Initial log: `/root/helix-h5-a346652/artifacts/h5-gate.log`.
This predates the review corrections below; affected acceptance must be rerun.

## Findings
| ID | Finding | Adjudication | Correction/status |
|---|---|---|---|
| H5-R1 | Aggregate checksum can hide cancelling or reordered numerical errors | VALID, IMPORTANT | Capture every timed result in bounded preallocated memory; compare all repeated tuples outside timing; independently validate all distinct row tuples at frozen tolerance. Added cancellation/reorder adversarial test. Rerun/re-review pending. |
| H5-R2 | Native payload not authenticated at each launch | VALID, IMPORTANT | Runner verifies and records SHA-256 before and after every child, including native; added payload-reorder rejection. Rerun/re-review pending. |
| H5-R3 | Reject or replace repetitions when runtime load/swap changes | INVALID as a mandatory gate | Preregistered contract requires recording load/swap and stable effective resource configuration, not constant load or post hoc exclusion thresholds. Preserve all scheduled runs, disclose load variation, and avoid selective reruns. Static CPU/affinity/memory/swap limits remain checked. |

Clean-checkout inspection also found that verify.sh lacked a tracked executable
bit. Corrected at 32c2730 before the successful initial gate.

## Re-review history
Copilot auto benchmark-auditor reviewed frozen 32c2730 with local read-only tools.
Log: `.agent-runs/20260910-152956-copilot-benchmark-auditor.md`. It reported the
three findings above; leader independently reconstructed and adjudicated them.
No test execution or milestone verdict is attributed to the reviewer.

## Technical verdict
IN_PROGRESS

## Learning checkpoint
PENDING_USER_DEMONSTRATION

## Handoff to next milestone
Not authorized by the technical gate yet.

