# H5 Review

## Scope and frozen contracts
See ARCHITECTURE.md, ROADMAP.md, EXPERIMENTS.md and H5_PACKET.md.
H4 TECH_PASS recorded at a13eca1; H5 implementation is authorized.

## Implementation handoff
- Candidate code commit: 1d8724c9521e7d88a43e6f751c4681e1fbbf75c6.
- Changes: Rust/C++ measurement drivers, bounded Linux campaign runner, independent
  numerical readback, provenance/RSS capture, analysis and CI smoke integration.
- Known limitations: one local WSL2 campaign; host load is not controlled,
  cgroup quotas are not separately recorded, and no general performance or
  hard-real-time claim follows. Remote CI and human learning remain separate.

## Acceptance matrix
| Requirement | Evidence | Result |
|---|---|---|
| Both FFI modes, prepared/pack, native reference, all N/B dimensions | 96 micro configurations, 576 runs; full tuple/repetition checks and NumPy oracle | PASS |
| Single-axis pipeline design, policies, loss honesty | 28 configurations, 168 runs; all raw Parquet validated; 84 exit-2 runs retained | PASS |
| Release, repetitions, seeded order, clean commit and Linux placement | campaign.json, recorded build commands/flags/hashes; one warmup plus five measurements | PASS |
| Provenance, resources, RSS, bounded artifacts | Per-run hashes/load/swap/RSS; stable 8 CPUs/~7.7 GiB/2 GiB; 830648366 artifact bytes <4 GiB | PASS |
| Honest analysis and raw evidence | Per-run latency percentiles, dispersion, complete schedule checks; SHA-256-verified portable archive | PASS |
| Prior regressions and mandatory sanitizers | 24 Rust tests, 42 H2 cases, 24 H3 runs, 7 H4 cases, native CTest and 533 NumPy cases per profile | PASS |
| Runner adversarial checks and Release smoke | Eight Python tests; 200 smoke runs on final candidate, explicitly non-evidence | PASS |
| Separate-context AI review and adjudication | Copilot plus Antigravity core review, Copilot final delta review; all valid findings corrected | PASS |

## Verification
Final candidate 1d8724c, Linux checkout `/root/helix-h5-a346652`, 2026-09-10:

- `HELIX_PYTHON=.venv/bin/python scripts/verify.sh H5`: exit 0. Durable log:
  [verification.log](../evidence/H5/verification.log). Smoke directory:
  `artifacts/h5-smoke-RcbbQ6/campaign`, 200 validated runs.
- `.venv/bin/python scripts/experiment.py --out artifacts/h5-campaign`: exit 0,
  744 validated runs, no dirty-tree entries. Builds plus campaign ran from
  18:45:00 to 18:51:24 UTC; seed 20260910. The runner invoked analysis afterward.
- The archive utility preserved 9627692 successful pipeline rows and all 7172308
  dropped attempts in counters (warmups included), plus every micro result record.
  All archive members were read back and SHA-256 checked. Index and archive hash:
  [raw-index.json](../evidence/H5/raw-index.json). No invented or excluded run.

Earlier initial/corrected gates at 32c2730/e6fa29e also passed but are superseded
by this final-candidate gate. Their logs remain in the original Linux artifacts.

## Findings
| ID | Finding | Adjudication | Correction/status |
|---|---|---|---|
| H5-R1 | Aggregate checksum can hide cancelling or reordered numerical errors | VALID, IMPORTANT | FIXED and re-reviewed. Every timed result is captured, all repeated tuples checked outside timing, and distinct tuples oracle-validated. Cancellation/reorder tests and final campaign pass. |
| H5-R2 | Native payload not authenticated at each launch | VALID, IMPORTANT | FIXED and re-reviewed. Payload SHA-256 before/after every child, including native; payload-reorder test and final campaign pass. |
| H5-R3 | Reject or replace repetitions when runtime load/swap changes | INVALID as a mandatory gate | Preregistered contract requires recording load/swap and stable effective resource configuration, not constant load or post hoc exclusion thresholds. Preserve all scheduled runs, disclose load variation, and avoid selective reruns. Static CPU/affinity/memory/swap limits remain checked. |
| H5-R4 | Manifest-only changes can alter processing configuration | VALID, IMPORTANT | FIXED and re-reviewed. Both hashes authenticated and retained; manifest-only mutation test and final campaign pass. |
| H5-R5 | Malicious change-and-restore can evade pre/post hashes | INVALID for this local trusted-input experiment | The runner creates and owns its corpus; concurrent hostile mutation/snapshot isolation is not a frozen requirement. Hashing is accurately described as before/after validation. A hostile same-user writer could also modify executables/results; adding a filesystem security boundary would expand scope. |

Clean-checkout inspection also found that verify.sh lacked a tracked executable
bit. Corrected at 32c2730 before the successful initial gate.

## Re-review history
Copilot auto benchmark-auditor reviewed frozen 32c2730 with local read-only tools.
Log: `.agent-runs/20260910-152956-copilot-benchmark-auditor.md`. It reported the
three findings above; leader independently reconstructed and adjudicated them.
No test execution or milestone verdict is attributed to the reviewer.

Copilot re-reviewed e6fa29e in a separate context, log
`.agent-runs/20260910-153608-copilot-benchmark-auditor.md`. It confirmed R1's
full capture/oracle correction and agreed with the leader's R3 adjudication.
It raised R4 and R5 above. These were reconstructed against the actual local
experiment contract; only R4 requires an implementation correction. Cgroup quota
metadata was identified as a limitation, not a demonstrated defect in this WSL
campaign. No constant-load guarantee is claimed.

Antigravity gemini-3.8-flash-high also completed a read-only AI review of e6fa29e,
log `.agent-runs/20260910-153630-agy-independent-reviewer.md`. It found no
BLOCKER/IMPORTANT issue in result capture/oracle checks, preparation parity or
payload authentication, and correctly left campaign evidence pending. Its
scope predates the manifest-authentication addition. Leader does not adopt its phrase "bit-exact": repeated
results use ordinary field equality, while oracle floats use the frozen tolerance.

Copilot's final frozen-delta review of 1d8724c, log
`.agent-runs/20260910-154137-copilot-benchmark-auditor.md`, confirmed R4 corrected,
agreed with the R5 scope adjudication, and found no BLOCKER/IMPORTANT regression.
All four completed core/delta review logs are preserved in `docs/evidence/H5/`.
These are AI reviews; none is a human learning or publication approval.

## Technical verdict
TECH_PASS — all executable acceptance, code-review and evidence/documentation
audit requirements are satisfied. Candidate 1d8724c. No code changes were made
during or after the measured campaign. Two MINOR documentation items (batch-FFI
qualifier in EXPERIMENTS.md and legacy H2 case count in ROADMAP.md) were
corrected during closeout.

## Learning checkpoint
PENDING_USER_DEMONSTRATION

## Handoff to next milestone
H6 remains NOT_STARTED. This task's authorized scope ends at H5; user learning and
publication remain pending and are not inferred from technical validation.

