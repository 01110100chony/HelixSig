# H0 Review

## Scope and frozen contracts
Independent allocation-free, reentrant C++20 kernel; deterministic corpus;
NumPy oracle; native tests; ASan/UBSan; initial CI definition. FFI/runtime/storage
are outside H0. See ARCHITECTURE.md for exact numerical and batch semantics.

## Implementation handoff
- Candidate code commit: ebc8edb312b2eab125dcc3b286fc880668a5b2a1
- Canonical candidate: Ubuntu-E:/root/helix; Windows mirror aligned before review.
- Changes: native kernel, generator, independent oracle, tests and CI definition.
- Known limitations: no remote CI execution or performance result claimed.

## Acceptance matrix
| Requirement | Evidence | Result |
|---|---|---|
| Standalone build and analytical/error cases | GCC Release + CTest | PASS |
| Independent numerical oracle | 256 generated + 6 adversarial NumPy cases | PASS |
| ASan/UBSan | Clang Debug CTest and 262 oracle comparisons | PASS |
| Formatting | clang-format dry-run | PASS |
| Separate-context review | See review history | PENDING |

## Verification
`HELIX_PYTHON=/root/helix/.venv/bin/python bash scripts/verify.sh H0` exited 0.
Ubuntu 24.04 WSL2, GCC 13.3, Clang 18. Evidence: artifacts/verification/h0.log.
Tolerance: abs(a-r)<=1e-10+1e-10*abs(r); exact indices/statuses.

## Findings
| ID | Severity | Classification | Evidence | Resolution |
|---|---|---|---|---|
| H0-N1 | BLOCKER | VALID | kernel.cpp linear reduction gives integral=-1 while NumPy 2.5.3 gives 0 on the cancellation fixture | Leader implemented blocked pairwise reduction; 533 comparisons pass; re-review pending |

Leader reproduced H0-N1 on the canonical Linux candidate using the pinned NumPy,
not only the reviewer's separate environment. No tolerance or formula change.

## Re-review history
- Initial Astra reviewer could not run because its usage limit was reached.
- External dispatch was initially blocked by automatic approval review.
- User explicitly approved required Helix file transfers to Antigravity/Copilot.
- Native Sol review identified H0-N1; the leader verified it independently.
- First permitted Antigravity run produced no review because a headless command
  lacked permission. This is NOT a clean review or approval.
- User reported CLI permission updates. Copilot correction is bounded to three
  explicit file-write permissions without shell access. Review will be repeated.
- Copilot produced no code: first attempt lacked write permission, second lacked
  the correctly named edit tool. Neither attempt counts as implementation/review.
- User requested leader-only implementation. The leader implemented the numerical
  correction and regression cases directly. No numerical tolerance was changed.
- Correction verification: scripts/verify.sh H0, exit 0; native and sanitizer
  CTest plus 533 NumPy comparisons per build. Evidence: h0-correction.log.
- The original reviewer was asked only to verify closure of H0-N1.

## Technical verdict
TECH_PASS. Leader approved candidate 3e6ca9f22833fe9a225b343db32a75b4a5db4a7e
after native/sanitizer gates and 533 independent NumPy cases per build passed.
The separate-context reviewer confirmed H0-N1 resolved and reported no introduced
substantive defect; its additional 640-case local sweep found no mismatch.
Limitation: reviewer did not inspect the Linux correction log; the leader did.

## Learning checkpoint
PENDING_USER_DEMONSTRATION

## Handoff to next milestone
H1 implementation authorized. Numerical/corpus contracts frozen at this gate.

