# H6 Review

## Scope and frozen contracts
Follow [H6_PACKET.md](H6_PACKET.md), [ARCHITECTURE.md](../ARCHITECTURE.md) and
[DEVELOPMENT.md](../DEVELOPMENT.md). H5 measured candidate is
`1d8724c9521e7d88a43e6f751c4681e1fbbf75c6`, technical closeout `6ba29f9`.
H5 remains closed. H6 changes release tooling, CI and documentation only.

## Implementation handoff
- Verified source candidate: `efed5d040f04c886d436e2b7fcd424930f597d49`;
  frozen AI-reviewed candidate `7915688502af9751119fb59f51459478a794cf69`,
  branch `codex/h6-release-candidate`. The diff efed5d0..7915688 contains only
  H6 evidence and its review record. The H6 release candidate is the commit
  containing this closeout and H6_HUMAN_PACKET.md. Its exact SHA and post-commit
  clean-room result are recorded in the external final attestation distributed
  with it; no runtime/verification behavior changes follow efed5d0.
- Changes: H6 alias/default for the existing full gate, explicit CMake test and
  sanitizer settings, pinned CI setup, offline reviewer-exit regression tests,
  executable wrappers and tracked Antigravity profiles, reproducibility docs,
  technical study and Portuguese human demonstration guide.
- Original Windows editing checkout has pre-existing user edits; they are
  preserved there and are not included in this separate H6 worktree.
- Limits: no runtime/kernel/FFI/benchmark behavior changes; no H5 campaign rerun.

## Acceptance matrix
| Requirement | Evidence | Result |
|---|---|---|
| Scope and inventory | H6_PACKET; native Codex inventory and external Copilot build audit | PASS |
| Initial clean Linux reproduction | New clone at 6ba29f9, fresh venv, full H5 command; baseline.log | PASS |
| Release tools and exit propagation | Offline two-success/seven-failure regression; exit-29 probe; old-bug reproduction | PASS on Git Bash and Linux |
| Local full H6 verification | Authoritative verify.sh H6 at efed5d0; local-verification.log | PASS |
| Compact CI aligned with local setup | ci.yml and DEVELOPMENT.md; local setup executed, both AI reviews | PASS locally; hosted Actions NOT_RUN |
| Docs, study, provenance and hygiene | README, study, H5 immutable metadata and release inventory; both AI reviews | PASS |
| Independent frozen-candidate AI review | Copilot/Antigravity at 7915688 plus primary adjudication; closeout delta is evidence/docs only | PASS |
| Pre-closeout clean room | New clone at 7915688, new venv and Cargo cache, full H6 gate and README examples | PASS |
| Final exact closeout commit | Same clean-room procedure after this immutable closeout; external attestation must name matching SHA | TECH_PASS is effective only with PASS attestation |
| Human-review packet | H6_HUMAN_PACKET.md plus exact-SHA external attestation/log | PASS when delivered together |

## Verification
Initial environment startup failed with WSL HCS/E_ACCESSDENIED when mounting
`C:\WSL\Ubuntu\ext4.vhdx`. The user started Ubuntu-E manually; Linux then became
accessible. This was an environment startup limitation, not a test failure.
Baseline uses `/root/helix-h6-baseline-20260911`, Ubuntu 24.04/WSL2, Linux
6.18.33.2, Rust 1.98.1, GCC 13.3, Clang/formatter 18.1.3, CMake 3.28.3 and
Python 3.12.3. The initial clone/venv contained no old build or ignored artifacts.

- `HELIX_PYTHON=.venv/bin/python scripts/verify.sh H5` at
  `6ba29f9df7a269ca1cee1090c6a40b170995fd0a`: exit 0,
  [baseline.log](../evidence/H6/baseline.log).
- Documented apt/rustup/pip setup completed. The first H6 attempt at a2414a3
  reproduced H6-R4 and was deliberately stopped/invalidated; its
  [partial log](../evidence/H6/invalidated-cache-run.log) is not acceptance evidence.
- `HELIX_PYTHON=.venv/bin/python scripts/verify.sh H6` at efed5d0: exit 0,
  2026-09-11 06:24:49-06:27:43 UTC;
  [local-verification.log](../evidence/H6/local-verification.log). New native
  trees selected the expected Release and instrumented Debug flags despite the
  incompatible old CMake trees. This local rerun reused Cargo/venv; final clean
  acceptance must still use another new clone and venv.
- Both suites passed 24 Rust tests, 42 H2 cases (4101-event readback per FFI
  mode), 24 H3 runs, seven H4 cases, eight Python runner tests, native CTest and
  533 NumPy cases in each native profile, formatting and Clippy, plus 200 smoke
  runs. H6 additionally passed release-tool regression. No full H5 campaign ran.
- Another clean clone at 7915688502af began with no build, target, artifacts or
  venv and used a new Cargo home plus pip with cache disabled. The full H6 gate
  exited 0 at 06:40:18 UTC, then the documented sequential and concurrent
  4101-event examples both completed with all counters reconciled and 4101 rows
  written. [Pre-closeout log](../evidence/H6/pre-closeout-cleanroom.log).
- [Hygiene/sanity audit](../evidence/H6/hygiene.md) records immutable H5 source/
  evidence, raw-archive hash, file modes/links, targeted inspection and failure
  propagation experiments. No missing fixtures or large generated tracked files.

## Findings
| ID | Finding / severity | Primary adjudication and action |
|---|---|---|
| H6-R1 | Agent wrappers have no tracked executable bit; IMPORTANT | VALID. Direct commands and nested helpers fail in Linux clones. Track executable modes. |
| H6-R2 | Antigravity profiles absent from Git; IMPORTANT | VALID. Track three bounded read-only profiles and remove their ignore rule. |
| H6-R3 | Multi-PID wait masks earlier reviewer failure; IMPORTANT | VALID. Wait every PID, accumulate failure, suppress completion on failure. Offline regression exercises every position. |
| H6-R4 | Cached CMake options can invalidate test/sanitizer profiles; IMPORTANT | VALID. Adversarial experiment found compiler changes discard same-call options: sanitized build became OFF and Release type empty. Invalidate/stop that gate. Use fresh native directories per invocation, explicit options and empty-CTest rejection, then rerun. General Cargo cache invalidity was not demonstrated; final acceptance uses a new clone. |
| H6-R5 | CI/setup relies on ambient tools and undocumented dependencies; IMPORTANT | VALID. Select Rust from the tracked TOML, fix compiler/formatter versions, document dependencies and use matching CI commands. |
| H6-R6 | check-tools.sh can report discovery even if optional probes fail; MINOR | VALID diagnostic limitation, deferred. It is a best-effort discovery aid, not an acceptance gate; actual reviewer execution must complete successfully. |
| H6-R7 | Stale H5 README/status, incomplete H6 study/packet; IMPORTANT for closeout | VALID. Reconcile current docs, preserve historical candidates and qualify raw evidence availability. |
| H6-R8 | No literal H6 entry point; native inventory called it BLOCKER | VALID gap, severity reduced to MINOR: existing H5 suite already covered executable contracts. Add H6 default/alias with release-tool checks, without a redundant wrapper. |
| H6-R9 | New packet is untracked during initial inventory; INFORMATIONAL | Expected pre-commit state, not a correctness defect. Candidate freeze requires all intended files committed. |
| H6-R10 | g0.sh prepends a developer-specific /root path; MINOR hygiene | VALID. Remove the stale path; setup documents the installed rustup PATH. G0 history/evidence remains unchanged. |
| H6-R11 | Final exact-SHA clean-room evidence pending at review time; IMPORTANT acceptance item | VALID. Clean clone at reviewed 7915688 passed. Reverify the immutable documentation closeout SHA and deliver the matching external attestation; no code correction required. |
| H6-R12 | Offline group regression replaces inner agy wrapper with a stub; MINOR | VALID coverage limit, deferred. The targeted regression attacks group failure aggregation; profile existence/modes are checked separately and the actual tracked agy wrapper completed the independent review. No demonstrated supported-path defect warrants more tooling. |
| H6-R13 | AI review includes stronger or imprecise explanatory wording; INFORMATIONAL | Primary does not adopt a universal deadlock-free proof, recoverability of native faults, or power-loss durability from static review. Invalid K is rejected before division, and summary-failure partial rename can itself fail. The technical study already states these bounds. No source change. |

R1/R2/R3/R8/R10 were corrected in 5d83ce2, R5 in b96cfba, R7 in a2414a3,
and the experimentally confirmed R4 in efed5d0. Final static re-review confirms
these fixes. Both reviewers repeat R6; classify those as DUPLICATE/MINOR, with
the documented deferral preserved. No unresolved implementation BLOCKER or
IMPORTANT remains. R11 is satisfied only by the matching post-commit external
attestation; absence or failure of that attestation makes this snapshot NOT_READY.
No proposed finding changes frozen numerical/runtime/FFI semantics. Python -O
and adversarial same-user file replacement remain outside the supported scope.

## Re-review history
Native Codex gpt-5.6-sol/medium, separate-context inventory (`h6_inventory`),
read-only: mapped setup, fixture, status and delegation gaps. Copilot auto
build-debugger reviewed the H5 base plus H6 packet, read-only, in
`.agent-runs/20260911-030928-copilot-build-debugger.md`. Its concrete findings
are adjudicated above. These are AI reviews, not human review or executed tests.
Copilot auto docs-auditor reviewed frozen 7915688 read-only on 2026-09-11:
[full review](../evidence/H6/20260911-copilot-final-review.md). It confirmed the
build/sanitizer, exit, CI and documentation corrections and raised R11/R12 plus
duplicate R6. Its NOT_READY statement is an advisory observation that gate E
was pending, not an independent milestone decision or newly discovered code bug.

Antigravity gemini-3.8-flash-high/high reviewed the same frozen candidate:
[full review](../evidence/H6/20260911-agy-final-review.md). It inspected Rust/C++
ownership, numerical/structural errors, bounded concurrency, shutdown, output,
exit status, release tools and the study against H5 evidence. No BLOCKER or
IMPORTANT finding; R6 repeated. Primary reconstruction agrees within the
documented scope and narrows broad reviewer language as R13. Neither reviewer
executed gates or provided human approval. Candidate source was frozen throughout.

## Technical verdict
H6 TECH_PASS, effective only for the immutable closeout SHA named by the matching
external PASS attestation. The primary orchestrator must run that exact commit
after this documentation commit and distribute the resulting log/packet with it.
If the SHA differs, the attestation is absent, or the command fails, the verdict
is H6 NOT_READY. This avoids a self-referential commit while preserving the rule
that nothing changes after final verification.

## Learning checkpoint
PENDING_USER_DEMONSTRATION

## Handoff to next milestone
See H6_HUMAN_PACKET.md. No merge, tag or publication is authorized by this
technical verdict. No post-H6 feature work follows.
